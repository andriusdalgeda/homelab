apiVersion: v1
kind: Service
metadata:
  name: cloudflared
  namespace: obsidian
spec:
  selector:
    app: cloudflared
  ports:
    - name: metrics
      port: 60123
      targetPort: 60123
  type: ClusterIP