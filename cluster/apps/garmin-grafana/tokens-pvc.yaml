apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garmin-grafana-tokens
  namespace: garmin-grafana
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
