apiVersion: v1
kind: Service
metadata:
  name: garmin-grafana-influxdb
  namespace: garmin-grafana
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: garmin-grafana-influxdb
  ports:
    - name: http
      port: 8086
      targetPort: http
      protocol: TCP
