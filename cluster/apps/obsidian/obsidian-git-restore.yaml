apiVersion: batch/v1
kind: Job
metadata:
  name: obsidian-git-restore
  namespace: obsidian
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: restore
          image: alpine/git:latest
          env:
            - name: GIT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: git-credentials
                  key: GIT_TOKEN
          command:
            - /bin/sh
            - -c
            - |
              set -e

              echo "Cloning repo..."
              git clone https://$GIT_TOKEN@github.com/andriusdalgeda/obsidian.git /repo
              
              cd /repo

              echo "Configuring git identity..."
              git config user.name "obsidian-restore"
              git config user.email "backup@local"

              echo "Restoring files..."
              rm -rf /data/*

              cp -a /repo/"Obsidian Vault"/. /data/

              echo "Restore complete"
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: obsidian-config