apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homepage
  labels:
    app: homepage
spec:
  selector:
    app: homepage
  ports:
    - name: http
      port: 4500
      targetPort: 4500
  type: ClusterIP
