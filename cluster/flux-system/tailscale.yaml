apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tailscale
  namespace: flux-system
spec:
  interval: 5m
  path: ./cluster/infrastructure/tailscale
  prune: true
  wait: true
  timeout: 10m
  dependsOn:
    - name: tailscale-secrets
  sourceRef:
    kind: GitRepository
    name: flux-system
